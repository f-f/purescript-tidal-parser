# purescript-tidal-parser

Parser for [TidalCycles](https://tidalcycles.org/)' mininotation.

See the `test` folder for examples on how to use the library.

Build with `spago build`, run tests with `spago test`
